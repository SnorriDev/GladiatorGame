using UnityEngine;
using System.Collections;

public class FallDamage : MonoBehaviour {

	/*public void OnCollisionEnter(Collision col) {

		Debug.Log (col.transform);
		
		#Debug.Log (transform.GetComponent<CharacterMotor> ().movement.velocity.magnitude);
		#if (transform.GetComponent<CharacterMotor>().movement.velocity.magnitude > 8)
		#	Debug.Log ("hello");
	}*/
}
